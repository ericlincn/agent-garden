# Design — <项目名称>

## 1. 架构总览

```
<ASCII 架构图：模块分层 + 数据流方向>
```

**分层契约**：
- <层1 名称>：<职责描述>
- <层2 名称>：<职责描述>
- <层3 名称>：<职责描述>
- 单向数据流：<input -> logic -> output 方向>

## 2. 模块拆分

### `<模块名>/`（<类型：纯函数 / 有状态 / 只读>）

| 文件 | 职责 | 关键签名 |
|---|---|---|
| `<file.ts>` | <职责> | `<函数签名>` |

## 3. 状态机（如适用）

```
<状态转换 ASCII 图>
```

## 4. 关键技术决策

### 4.1 <决策名称>

- **Why**: <选择理由>
- **Alternatives considered**: 
  - <替代方案 A>：<未选理由>
  - <替代方案 B>：<未选理由>
- **Trade-off**: <取舍说明>
- **Referenced pattern**: <参考的设计模式或 GitHub 范例（见 references/）>

> 仅关键决策需要 Rationale。普通实现细节不需要。

## 5. 契约边界

- <模块A> 接受 plain object，返回 plain object；不抛异常，碰撞 / 失败返回 null 或 false
- <模块B> 拥有所有可变状态；不允许外部直接修改内部字段
- <模块C> 仅通过 readonly 接口读取状态，不修改任何字段
- <模块D> 仅在特定状态下生效；其他状态忽略输入

## 6. 不在 design 内的细节

- 逐条约束规则见 `contracts/`
- 需求列表（SHALL + Scenario）见 `specs/<capability>/spec.md`
- 任务拆解与施工进度见 `phases/`

## 7. 风险与缓解

| 风险 | 缓解 |
|---|---|
| <风险描述 1> | <缓解措施> |
| <风险描述 2> | <缓解措施> |
